Паттерн Фасад позволяет скрыть сложность системы путём сведения всех возможных внешних вызовов к одному объекту,
делегирующему их соответствующим объектам системы.

Представить единый интерфейс вместо нескольких

The disease façade pattern with a latent disease of the system leads to the detection of all cases of infections in one object,
delegating them to a suitable system object.

Assume an interaction instead of a few